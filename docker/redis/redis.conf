appendonly yes
appendfsync everysec
save 60 1000
# Redis is not published to the host; it is reachable only on the private
# Compose network by the API. Host binding is intentionally absent in Compose.
bind 0.0.0.0
protected-mode no
