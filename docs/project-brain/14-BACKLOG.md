# 14 - BACKLOG

## Important

Local embedding model if benchmarks justify; local reranker if benchmarks
justify; binary vector quantization; Zstd dictionary training; semantic response
cache; more language parsers; advanced risk-based context modes.

## Future

Distributed HIVE, multi-user/organizations, advanced RBAC/SSO, Kubernetes,
knowledge graph, cloud synchronization, managed cloud edition, fine-tuned
executor models, cross-project approved shared memory, advanced mobile Control
Center, autonomous optimizer trained from benchmark history, provider routing by
price/quality/latency, and agent scheduling/long-running workflows.

Backlog items do not enter V0.1 automatically. They require reclassification as
NECESSARY based on evidence.
